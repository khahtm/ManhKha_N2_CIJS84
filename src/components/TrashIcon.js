import React from 'react';

const TrashIcon = ({ onClick }) => {
  return (
    <span className="trash-icon" onClick={onClick}>
      {/* Add trash icon SVG or font icon */}
    </span>
  );
};

export default TrashIcon;
